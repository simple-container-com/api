# Generated by sc CLI when initially setting up
schemaVersion: 1.0
stacks:
  staging: &staging
    type: compose
    parent: refapp
    environment: staging
    domain: staging.sc-refapp.org
    config:
      docker-compose-file: ./docker-compose.yaml # uses listed containers from docker-compose.yaml
      uses:
        - mongodb
      runs:
        - api
        - ui
  prod:
    <<: *staging
    environment: prod
    domain: prod.sc-refapp.org