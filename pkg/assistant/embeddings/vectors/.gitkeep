# This directory contains prebuilt embeddings files
# Files that can be placed here:
# - prebuilt_embeddings_openai.json (OpenAI embeddings)  
# - prebuilt_embeddings_local.json (Local model embeddings)
#
# These files are generated by the generate-embeddings command
# and embedded into the binary at build time for offline operation.
