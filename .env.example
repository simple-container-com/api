# Simple Container AI Assistant Configuration

# OpenAI API Configuration (for embeddings generation and chat)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Embedding Model Selection (optional)
# Supported models:
# - text-embedding-3-small (default, cheapest, 1536 dimensions)
# - text-embedding-3-large (better quality, 3072 dimensions) 
# - text-embedding-ada-002 (legacy model)
SIMPLE_CONTAINER_EMBEDDING_MODEL=text-embedding-3-small

# AI Assistant Configuration (optional)
SIMPLE_CONTAINER_LLM_MODEL=gpt-3.5-turbo
SIMPLE_CONTAINER_MAX_TOKENS=4000
SIMPLE_CONTAINER_TEMPERATURE=0.7
